.DS_Store
node_modules/

# Artisan auditor working state (per-project ledgers) — never committed
.artisan/

# codebase-guardian validation-hook suggestion staging (transient, machine-local)
.guardian/
